apiVersion: v1
kind: Service
metadata:
  name: chat-demo-frontend-svc2
spec:
  type: LoadBalancer
  selector:
    app: chat-demo-frontend-dep
  ports:
    - protocol: TCP
      port: 88
      targetPort: 80
